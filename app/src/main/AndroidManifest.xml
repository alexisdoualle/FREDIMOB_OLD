<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.owulanii.androidlogin">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".AndroidLoginController"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"/>
        <activity android:name=".SignUp"/>
        <activity android:name=".Login" android:label="@string/app_no_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".AjoutFrais" android:parentActivityName=".MainActivity" />

        <activity android:name=".AfficherFrais" android:parentActivityName=".MainActivity" />

        <activity android:name=".DetailFrais" />

        <activity android:name=".ModifierInfosPerso" android:parentActivityName=".MainActivity"/>




    </application>

</manifest>